---
tls_san:
  # Ancien: "IP:10.255.255.253"  ← IP hardcodée, utilise ansible_host (vient de config.env via l'inventaire)
  - "IP:{{ ansible_host }}"
  - "IP:127.0.0.1"
  - "DNS:bastion"
  - "DNS:localhost"
  - "DNS:kibana"
